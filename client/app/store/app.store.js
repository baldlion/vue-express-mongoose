const state = {
  title: 'App'
}

const getters = {
  title: state => state.title
}

const mutations = {}

const actions = {}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}
